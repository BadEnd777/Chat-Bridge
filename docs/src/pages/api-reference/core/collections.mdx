import { Cards, Card } from 'nextra/components'
import { FaPlus, FaSearch, FaSearchPlus, FaTrash, FaTrashAlt, FaKey } from 'react-icons/fa'

# Collections: Class

The `Collections` class is a generic class for managing collections of items. It is designed to work with objects that
implement the `Identifiable` interface.

## Usage

```js copy
const { Collections } = require('chat-bridge')
```

## Constructor

To create a new `Collections` instance, use the following syntax:

```js copy
const collections = new Collections()
```

## Methods

<Cards>
    <Card icon={<FaPlus />} title="Add" href="#add" alt="Add" arrow />
    <Card icon={<FaSearch />} title="Get" href="#get" alt="Get" arrow />
    <Card icon={<FaSearchPlus />} title="GetAll" href="#getall" alt="GetAll" arrow />
    <Card icon={<FaTrash />} title="Remove" href="#remove" alt="Remove" arrow />
    <Card icon={<FaTrashAlt />} title="RemoveAll" href="#removeall" alt="RemoveAll" arrow />
    <Card icon={<FaKey />} title="GetKey" href="#getkey" alt="GetKey" arrow />
</Cards>

### Add

-   `add(item: T): void` - Adds an item to the collections.
    -   `item`: The item to be added.

### Get

-   `get(key: string): T | undefined` - Gets an item from the collections based on the provided key.
    -   `key`: The key of the item to retrieve.
    -   **Returns:** The retrieved item or `undefined` if not found.

### GetAll

-   `getAll(): T[]` - Gets all items from the collections.
    -   **Returns:** An array containing all items in the collections.

### Remove

-   `remove(key: string): void` - Removes an item from the collections based on the provided key.
    -   `key`: The key of the item to be removed.

### RemoveAll

-   `removeAll(): void` - Removes all items from the collections.

### GetKey

-   `getKey(item: T): string` - Gets the key of an item.
    -   `item`: The item for which to retrieve the key.
    -   **Returns:** The key of the provided item.

## Example

```js copy
// Instantiate a new Collections
const collections = new Collections()

// Add some items
collections.add({ id: '1' })
collections.add({ id: '2' })
collections.add({ id: '3' })

// Log all items
console.log(collections.getAll()) // [{ id: '1' }, { id: '2' }, { id: '3' }]

// Get an item by key
console.log(collections.get('2')) // { id: '2' }

// Attempt to get an item with an invalid key
console.log(collections.get('4')) // undefined

// Remove an item by key
collections.remove('2')
console.log(collections.getAll()) // [{ id: '1' }, { id: '3' }]

// Remove all items
collections.removeAll()
console.log(collections.getAll()) // []
```
